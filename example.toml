db_path = ".\\databases\\" # 解密后的数据库目录路径
pic_path = ".\\chatpic\\" # chatpic目录路径，留空不导出完整路径

output_path = "" # 导出的路径，留空默认为数据库上级目录

c2c_filters = [] # 需要输出的私聊消息的QQ号列表，留空导出全部
group_filters = [] # 需要输出的群聊消息的群号列表，留空导出全部

# 导出格式：chatlab_json（ChatLab JSON 格式）或 chatlab_jsonl（ChatLab JSONL 流式格式）
output_format = ["chatlab_json", "chatlab_jsonl"]
